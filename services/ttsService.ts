
// This file is now deprecated in favor of useGeminiLive for real-time commentary.
// Kept empty to avoid import errors if still referenced elsewhere.
export const announceWinner = async (name: string) => {
  console.log("Announcement handled by Live AI Commentator: ", name);
};
